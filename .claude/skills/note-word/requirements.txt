# 必須（記事生成・Word化・図解生成）
python-docx>=1.1.0,<2
Pillow>=10.0.0,<12
playwright>=1.40,<2

# Playwright インストール後、初回のみ Chromium をダウンロードする：
#   playwright install chromium
# （約200MB。これをやらないと図解・サムネが生成できない）

# 任意拡張: nanobanana-deji（Gemini API）を使いたい上級者のみ：
#   pip install -r requirements-nanobanana.txt
# 通常運用では不要（Playwright経路で十分）
