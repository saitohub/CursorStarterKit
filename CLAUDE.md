# CLAUDE.md

This file provides guidance to Claude Code (claude.ai/code) when working with code in this repository.

## このリポジトリについて

Obsidian Vault と Python スクリプトを組み合わせた、コンテンツ制作・知識管理のための個人ワークスペース。発信者ハンドルは**サイジム**。

## スクリプトの実行方法

`scripts/` 以下に Gmail API を使った2つのスクリプトがある。実行はリポジトリルートから行う。

```bash
# イチゼミ・アカデミア・イチトレ・イチヘリのメールを収集 → ichimaiworks_list.csv
python scripts/fetch_ichimaiworks.py

# イチラボのメールを収集 → lecture_list.csv
python scripts/fetch_ichilabo.py
```

**認証ファイルの場所：** `client_secret_*.json` を `~/secrets/gmail_api/` に置く。初回実行時にブラウザ認証が走り、`~/secrets/gmail_api/token.json` が生成される。以降は自動更新。認証ファイルはVault外に保管するためGitには含まれない。

依存ライブラリ: `google-auth`, `google-auth-oauthlib`, `google-api-python-client`

## フォルダ構成と役割

| フォルダ | 役割 |
|---------|------|
| `01_Notes/` | 一次置き場。メモ・アイデア・下書き |
| `02_Configs/Templates/` | Obsidianテンプレート（編集不要） |
| `03_Knowledge/` | 自分の知識資産（Story / Logic / Evidence / Claim） |
| `04_Journals/` | 日々のジャーナル（ファイル名：`YYYY-MM-DD.md`） |
| `05_Outputs/` | 完成コンテンツ。noteは `05_Outputs/note/` 以下に保存 |
| `06_Reflections/` | 週次・月次振り返り（Weekly / Monthly） |
| `07_Swipe/` | **他人の**事例ライブラリ（構造参考のみ） |
| `.cursor/rules/` | AIへの永続指示（my-business.mdc が中心） |
| `.cursor/commands/` | Cursorスラッシュコマンドの定義 |
| `scripts/` | Gmail APIスクリプト |

## コンテンツ制作の重要ルール

### 07_Swipe は「構造の教科書」（swipe-guard）

`07_Swipe/` の中身は**全て他人の実績・文章・事例**。

**絶対に守ること：**
- Swipe内の売上数字・成果・体験談をサイジム自身のものとして出力しない
- Swipe内のエピソード・過去の経験を、一人称体験として書かない
- Swipe内の文章をそのまま転用しない

**やっていいこと：**
- 構造・構成パターンの分析と参考
- 表現テクニック・切り口の学習
- 自分のコンテンツとの比較分析

コンテンツ素材は必ず `03_Knowledge/`（自分の知識資産）から引く。`07_Swipe/` は構造を学ぶためだけに参照する。

### セット号（ファイル名プレフィックス）ルール

ファイル名の先頭にセット号を付けることで、AIがファイルの中身を読まずに保存先・種類・タグを判断できる。規格がないと自動化のコストが増大する（AI分身ラボ第5講）。

**新規ファイルは必ず以下のセット号を使う。既存ファイルは旧プレフィックスのまま。**

#### 05_Outputs/note/（noteコンテンツ）

| セット号 | 対象 | 命名例 |
|---------|------|--------|
| `note-記事-` | 無料note（下書き〜公開用） | `note-記事-2026-06-09-タイトル.md` |
| `note-有料-` | 有料note（下書き〜公開用） | `note-有料-20260609-タイトル.md` |
| `note-貼付-` | note.comにコピペする清書版 | `note-貼付-2026-06-09-タイトル.md` |
| `note-固定-` | プロフィール・自己紹介など固定ページ | `note-固定-プロフィール.md` |

note の記事・原稿は `05_Outputs/note/` 以下に保存する（`05_Outputs/` 直下には置かない）。

#### 05_Outputs/その他

| セット号 | 対象 |
|---------|------|
| `mail-` | メルマガ・メール原稿 |
| `sns-` | SNS投稿（X・Threads・インスタ） |
| `promo-` | プロモーション素材 |

#### 03_Knowledge/（変更なし・維持）

`Story -` / `Logic -` / `Claim -` / `Evidence -`

#### 07_Swipe/（変更なし・維持）

`Swipe -`

### ターゲット読者（健一さん）

有料note・無料noteは**田中健一さん（47歳・多忙な会社員）**に語りかける形で書く。

**核心ストーリー：「ミッションを分解したら、元に戻せなくなった」**  
やる気はあり、講座にも課金した。でも自分の核を整理しようとしたとき、バラバラになって元の輪郭が見えなくなった。設計しようとするほど手が止まる、の繰り返し。

| 項目 | 内容 |
|------|------|
| 属性 | 中小企業の会社員（30〜50代）。昇進は厳しいと感じ、新しい収益源を作りたい |
| 副業状況 | noteやSNSに投稿しているが反応がない。「こんなことやって成果出るのか」が本音 |
| AI | ChatGPT無料は使う。AIで記事は書けるようになったが誰にも届いていない感覚 |
| 時間 | 学び・副業は夜22時以降の30〜45分だけ |
| 表面の痛み | 学びの蒸発／反応ゼロで落ち込む／完璧主義で出せない／設計がバラバラで動けない |
| 深い痛み | 整理しようとするほど迷子になる。積み上がっていない感覚。「過去の呪い」がブレーキ |
| 本当に欲しいもの | まず100円でもいい手応え。自分のノウハウを明確な目的地に沿って動かせる設計 |
| 買う直前の心の声 | 精神論よりワークとチェックリスト。**開発者向けではない**と冒頭3行で知りたい。煽り・月収断定は信用しない |

冒頭3行で「誰向け・何が残る・開発者向けではない」を示す。煽り・月収断定・根拠なき断言は禁止。

### 文体・トーンのルール

- 話し言葉ベース。堅すぎる言い回しは避けて、短く伝える
- 感情が乗りやすい前提で、感情だけで押し切らず「事実と根拠」を必ず置く
- AIっぽい表現（「〜ということだ」「〜に他なりません」など）は使わない
- 構造パターン：体験（何が起きたか）→ 気づき（なぜそうなったか）→ 教訓（次どうするか）→ **次の一行**
- 冒頭に「こんにちは。サイジムです。」などの一行を置く
- 書き出しで「何がわかる記事か」を先に言う

### 禁止事項

- 煽り系の表現はしない
- 不安を過剰に刺激して行動を迫る書き方はしない
- 根拠のない断定はしない
- 感情だけで押し切る文章にしない
- 読者を置いていく上から目線の言い方はしない
- 禁止事項は5〜10個を超えて増やさない（増やしすぎると出力が無難になる）

### 着地点のルール

多忙な会社員でも、手足を動かすだけで動き続ける自分になれる。  
認識：「悩む前に小さく動く」。行動：「まず1つ実行」。状態：「止まらない自分」に着地させる。  
毎回同じ決め文句は使わず、最後は必ず「次に何を動かすか」が明確になる締め方にする。

## 作業規律

### ファイル保存の明示（file-save-confirmation）

ファイルを新規作成・上書き保存したときは必ず：
1. 「保存しました」または「更新しました」と明示
2. 作成・変更したファイルの絶対パスを列挙
3. Finderで確認できる旨を一言添える

チャットに要約や下書きを出しただけの場合は「まだファイルには書いていません」とはっきり書く。

### ジャーナルのfrontmatter（journal-frontmatter）

`04_Journals/` のファイルを作成・編集するとき、先頭のfrontmatterは以下の形を一字一句守る：

```yaml
---
type: journal
date: YYYY-MM-DD
tags: [journal]
---
```

frontmatter内にMarkdown見出し（`##`）を入れない。`type`/`date`/`tags`の行を削除・並べ替えしない。

### ファイル読み込みの規律（read-discipline）

- スキルや指示で「リファレンスとして読む」と指定されたファイルは**全文を読む**（offset/limit使用禁止）
- 「冒頭だけ読んで雰囲気を掴む」「要約読みで済ませる」は禁止
- 編集対象のファイルは、編集前に必ず一度全体を確認する

### 収益CEO視点（revenue-ceo）

サイジムはnoteに37本・執筆100時間を費やして収益はゼロ（2026年5月時点）。準備・学び・整理が目的化するリスクがある。

会話のたびに内部で問う：
1. **この作業は30日以内に売上に繋がるか？** NOなら先に伝える
2. **「整理・準備・学び」ループに入っていないか？**
3. **今あるアセットで明日から売れるものは何か？**

以下のパターンが見えたら一言挟む：

| パターン | 一言 |
|---------|------|
| ノート整理・Vault構造の話 | 「それは今週の売上に必要か？」 |
| 新しいテンプレ作成 | 「既存のもので出せないか？」 |
| 記事のクオリティ上げを繰り返している | 「80点で出す。完璧は売れてから直す」 |
| 来月・来週に先送り | 「今週出せる形は何か？」 |

次のマイルストーン：**初収益（1円でも）**。そのために今週何を出すかだけ考える。

## .cursor/rules の主要ファイル（参照元）

- `my-business.mdc` — ビジネス設計の詳細版（上記ルールの元ファイル）
- `swipe-guard.mdc` — 07_Swipeルールの元ファイル
- `file-save-confirmation.mdc` / `journal-frontmatter.mdc` / `read-discipline.mdc` / `revenue-ceo.mdc` — 上記各ルールの元ファイル

## note-word スキルの使い方

`簡易型Note作成くん` スキルを使って note 記事を生成できる。スキルは Vault の `.claude/skills/note-word/` に配置済み。

### 実行方法

Claude Code の作業ディレクトリを **このVault（CursorStarterKit）** にした状態で呼び出す：

```
note-wordスキルで「〇〇」の記事を書いて
```

### 出力先ルール

生成された記事フォルダ（`YYYYMMDD_slug/`）は必ず **`05_Outputs/note/`** に保存する。
ツール側の `output/articles/` に出力された場合も、完成後に `05_Outputs/note/` へ移動すること。

### 依存ライブラリ（初回のみ）

```bash
pip install -r .claude/skills/note-word/requirements.txt
playwright install chromium
```

## 利用可能なコマンド（Cursor commands → Claude Codeでの代替）

| コマンド | 内容 |
|---------|------|
| `/weekly-journal` | 週次振り返り生成 |
| `/monthly-journal` | 月次振り返り生成 |
| `/journal-fix` | ジャーナル修正 |
| `/product-create` | 商品企画の壁打ち |
| `/promotion-scaffold` | プロモーション配信メール一括生成 |
| `/setup-target` | ターゲット設計インタビュー（my-business.mdc更新） |
| `/setup-character` | キャラクター設計インタビュー（my-business.mdc更新） |
| `/threads-post` | Threads投稿スレッドのドラフト生成 |
| `/fact-check` | 公開前のnote記事のファクトチェック（数字・AIツール情報・体験矛盾・断言・トーン違反の5項目） |
