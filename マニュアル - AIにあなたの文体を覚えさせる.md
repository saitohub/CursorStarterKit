# AIにあなたの文体を覚えさせる

> AIの出力が「なんか自分っぽくない」をゼロにする方法を解説します。設定ファイルを育てる + 壁打ちで修正を重ねる + Swipeから理想のパターンを学ばせる。この3つの組み合わせで、AIがあなたの分身になります。

**関連マニュアル：**
- [はじめにお読みください](はじめにお読みください.md) — 初期設定（`/setup-character` でまず基本を入れる）
- [Swipeから理想のアウトプットを作る仕組み](マニュアル%20-%20Swipeから理想のアウトプットを作る仕組み.md) — 他人の事例から構造パターンを学ばせる方法
- [Cursor基本操作ガイド](マニュアル%20-%20Cursor基本操作ガイド.md) — Cursorの基本操作

---

## なぜAIの出力が「自分じゃない」のか

| 原因 | 具体的に何が起きるか |
|------|------------------|
| あなたの情報がない | 毎回「初対面のライター」に依頼してるのと同じ |
| 文体の指定がない | AIのデフォルト＝丁寧語で均一な「教科書文体」 |
| 禁止事項がない | AIが勝手に使う「〜の重要性」「〜という想いが芽生えた」系の表現 |
| 修正が蓄積されない | 毎回同じ直しをし続ける（永遠に70点） |

解決策は3つのレイヤーで攻める：

```
レイヤー①：設定ファイルに書く（my-business.mdc）
レイヤー②：壁打ちで修正を繰り返す
レイヤー③：Swipeから理想のパターンを学ばせる → ルール化・スキル化
```

---

## レイヤー①：設定ファイルに書く

### my-business.mdc の文体セクションを埋める

`/setup-character` を実行すると、以下が自動で設定される：

| 設定項目 | 入れる内容 | 例 |
|---------|----------|-----|
| 一人称 | 僕 / 私 / 俺 | 「僕」 |
| 基本トーン | 口語寄り / 丁寧語 / 混合 | 「話し言葉ベース。関西弁が混じる」 |
| 使う表現 | 自分がよく使うフレーズ | 「〜っすよ」「マジで」 |
| 禁止表現 | AIが使いがちだけど自分は使わない表現 | 「〜の重要性」「〜を痛感した」 |
| 着地点 | コンテンツの結論がいつも向かう方向 | 「構造を設計しろ→自動化しろ」 |

### 手動で追記してもOK

`/setup-character` で基本を入れたあと、`.cursor/rules/my-business.mdc` を直接開いて追記できる。

追記の例：
```
### AI臭を消すルール
- 感情を綺麗な動機に整理しすぎない（雑で乾いた本音を残す）
- 均一に丁寧な文体にしない（崩れた口語を意図的に混ぜる）
- 核心ほど崩す（整った言葉で核心を語るとAI臭が増す）
- 「〜という想いが芽生えた」「〜の重要性」は禁止
```

---

## レイヤー②：壁打ちで修正を繰り返す

設定ファイルだけでは限界がある。**実際に書かせて → 直す → その直しを蓄積する**のが最強。

### 基本の壁打ちフロー

```
① AIにコンテンツを書かせる（メルマガ、SNS、台本等）
   ↓
② 「自分じゃない」と感じる箇所を指摘する
   ↓
③ AIが修正する
   ↓
④ ②〜③を2〜3回繰り返す
   ↓
⑤ 修正パターンが溜まったら → my-business.mdc に追記
```

### 指摘の仕方（具体例）

AIに伝えるフィードバックは、具体的であるほど効く：

| ❌ 曖昧なフィードバック | ✅ 具体的なフィードバック |
|---------------------|---------------------|
| 「なんか違う」 | 「この部分、丁寧すぎる。もっと崩して」 |
| 「自分っぽくない」 | 「"痛感しました"は使わない。"マジでそれ思った"くらいの温度で」 |
| 「もっと良くして」 | 「冒頭のエピソードをもっと生々しく。数字と場面を入れて」 |

### 修正を蓄積する方法

何度も同じ直しをしていることに気づいたら、それをルール化する：

```
AIへの指示例：
「今の修正パターンを分析して、my-business.mdc の文体セクションに
 追記すべきルールとしてまとめて」
```

AIが修正履歴を分析して、ルールを提案してくれる。
あなたが確認して、OKならそのまま my-business.mdc に反映する。

---

## レイヤー③：ルールやスキルとして仕組み化する

修正が何度も繰り返されるパターンは、**ルールファイルやスキルファイルにして自動化**できる。

### ルールファイル（.cursor/rules/）とは

AIに「常に守らせたい指示」を書いておくファイル。
`my-business.mdc` がその代表例。

**用途別にルールを分けることもできる：**

| ファイル名 | 用途 | 発動条件 |
|----------|------|---------|
| `my-business.mdc` | ビジネス全般の設定 | 常に適用 |
| `swipe-guard.mdc` | Swipeフォルダの保護 | `07_Swipe/` を参照した時 |
| `mail-style.mdc`（新規）| メルマガ専用の文体ルール | メルマガ作成時に `@` で指定 |
| `sns-style.mdc`（新規）| SNS投稿専用の文体ルール | SNS投稿作成時に `@` で指定 |

### ルールファイルの作り方

1. `.cursor/rules/` フォルダに `.mdc` ファイルを新規作成
2. ファイル冒頭に以下のヘッダーを入れる：

```
---
description: このルールの説明（1行）
globs:
alwaysApply: true
---
```

| 設定 | 意味 |
|------|------|
| `alwaysApply: true` | 常に適用される |
| `alwaysApply: false` + `globs: 05_Outputs/Mail Magazine/**` | 特定フォルダを触るときだけ適用 |
| `alwaysApply: false` + globs なし | `@ファイル名` で手動参照したときだけ適用 |

3. ヘッダーの下にルール（指示文）を書く

### ルール化の具体例

壁打ちで「メルマガの文体」について何度も同じ修正をしている場合：

```markdown
---
description: メルマガ専用の文体ルール
globs:
alwaysApply: false
---

# メルマガの文体ルール

## 構成
- 1行は40文字以内（スマホで読みやすく）
- 3〜4行ごとに空行を入れる
- セクション見出しは ── で区切る
- PSは必ず入れる

## 文体
- 書き出しはいきなり問いかけ or エピソード
- 「こんにちは」「お疲れ様です」等の挨拶は入れない
- 核心の一文は **太字** にする
- 最後は読者への問いかけ or 行動の提案で終わる
```

メルマガを書くときに `@mail-style.mdc` と参照すれば、このルールが適用される。

### スキルファイル（.cursor/skills/）とは

ルールが「守らせたいこと」なら、スキルは「やらせたいこと」。
複数ステップの作業手順をまとめたもの。

すでにスターターキットに入っている例：
- `.cursor/skills/journal-crystallizer/SKILL.md` — ジャーナルの結晶化スキル

スキルの作り方の詳細は → [Swipeから理想のアウトプットを作る仕組み](マニュアル%20-%20Swipeから理想のアウトプットを作る仕組み.md) で解説。

---

## 文体が育つまでのロードマップ

| 時期 | やること | AIの精度 |
|------|--------|---------|
| 初日 | `/setup-character` を実行 | 50〜60点（一般的な文体） |
| 1週目 | 3〜5本書かせて修正を繰り返す | 65〜70点 |
| 2週目 | 修正パターンを my-business.mdc に追記 | 75〜80点 |
| 1ヶ月 | 用途別ルールファイルを作成 | 85点〜 |
| 3ヶ月 | 蓄積された素材とルールで「あなた化」が完成 | 90点〜 |

**最初は70点で当たり前。**
設定ファイルに書くよりも、壁打ちで修正を繰り返して蓄積するほうが早い。
ただし蓄積を仕組み（ルール化）に変えないと、毎回同じ修正を繰り返すことになる。

---

## 実践：文体を最速で覚えさせるワークフロー

### ステップ1：自分で書いた文章を食わせる

過去に自分で書いたメルマガ・ブログ・SNS投稿があれば、`07_Swipe/` ではなく **`01_Notes/` に自分の過去作品として保存** する。

```
AIへの指示：
「@01_Notes/自分の過去メルマガ3本.md を読んで、
 私の文体の特徴を箇条書きで分析して。
 口癖、リズム、構成パターン、感情表現の癖を抽出して」
```

### ステップ2：分析結果を my-business.mdc に反映

AIが出した分析結果を確認して、合っているものを my-business.mdc に追記。

### ステップ3：新しいコンテンツを書かせて比較

```
「さっき分析した文体ルールに従って、
 ○○のテーマでメルマガを書いて。
 自分の過去メルマガと同じトーンで」
```

違和感があれば指摘 → 修正 → ルールに追記。
このサイクルを3〜5回繰り返すだけで、劇的に精度が上がる。
