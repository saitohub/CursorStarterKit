# データバックアップ

> このスターターキット（Obsidian Vault 兼 Cursor ワークスペース）のノート・設定・下書きを失わないための方針と手順です。

**まず結論：** 継続的なバックアップには **GitHub への自動同期（Obsidian の Git プラグイン）** を推奨します。手順の細部は次のマニュアルに任せ、このページでは「何を守るか」「他にどんな手段があるか」を整理します。

- **詳細手順（初回セットアップ・HTTPS 認証・.gitignore・チェックリスト・トラブルシュート・`git clone` 復元）** → [データのバックアップ（GitHub設定）](マニュアル%20-%20データのバックアップ（GitHub設定）.md)

---

## 1. 何をバックアップの対象にするか

このフォルダ一式が「知的資産の本体」です。特に次の場所は優先度が高いです。

| 場所 | 中身の例 |
| --- | --- |
| `03_Knowledge/` | Story / Logic / Evidence / Claim（自分の知識資産） |
| `04_Journals/` | 日々のジャーナル |
| `05_Outputs/` | 記事・note原稿・商品関連 |
| `06_Reflections/` | 週次・月次の振り返り |
| `.cursor/rules/` | `my-business.mdc` など AI への永続指示 |
| `01_Notes/` | 下書き・アイデアメモ |

`07_Swipe/` は他人の参考事例です。失っても致命的ではない一方、溜めた分は手間なので **同じバックアップ先に含めて問題ありません**。

---

## 2. バックアップの考え方（最低限）

よく言われる **3-2-1** のイメージだけ押さえておけば十分です。

- **3** … データを **3** コピー（原本＋バックアップが合計3つあると安心、という目安）
- **2** … **2** 種類のメディア（例：PCのディスク ＋ クラウド）
- **1** … そのうち **1** つは **別の場所**（家の外＝GitHub やクラウド、別ドライブなど）

このキットだけなら、「PC上の Vault」＋「GitHub のプライベートリポジトリ」で 2 種類・別場所の要件をかなり満たせます。

---

## 3. 推奨：GitHub ＋ Obsidian Git プラグイン

- 変更履歴が残り、誤削除や上書きの復元にも使える
- プライベートリポジトリにすれば非公開のままクラウドに置ける
- 設定後は **定期的に自動 push** できる（手間が少ない）

**具体的なコマンド・画面操作はすべてこちらに集約しています。**

→ [データのバックアップ（GitHub設定）](マニュアル%20-%20データのバックアップ（GitHub設定）.md)

---

## 4. 補助的な手段（状況に応じて）

### 4.1 OS 標準のバックアップ

- **macOS** … Time Machine（外付けディスクや NAS）。Vault フォルダが含まれるボリュームを対象にする。
- **Windows** … バックアップと同期、またはファイル履歴、OneDrive の「重要なフォルダー」など。

OS バックアップは「丸ごと巻き戻し」に強い一方、**Obsidian と Git と同時に触ると競合**しやすいので、Vault は「Git でクラウド」「Time Machine は丸ごと」など役割を分けると安全です。

### 4.2 クラウドストレージ（Dropbox / iCloud Drive / OneDrive など）に Vault ごと置く

- メリット：設定が簡単なことが多い。
- **注意**：Obsidian を複数端末で開きっぱなしにすると、同期のタイミングで **同じファイルの競合コピー**（`ファイル (競合1).md` など）が起きることがあります。  
  **Git で一元管理する方が安定**なケースが多いです。クラウドに置くなら「1台だけが主編集」「他は閲覧中心」などルールを決めるとよいです。

### 4.3 手動コピー（ZIP や外付け SSD）

- 大きな変更の前・納品前などに「この日時のスナップショット」を取りたいときに有効です。
- Finder / エクスプローラーでフォルダを丸ごとコピーするか、圧縮して別ドライブに保存します。
- **欠点**：更新のたびに手作業が必要。メインの自動手段とは別に「保険」として使うのが現実的です。

### 4.4 Obsidian Sync（有料公式サービス）

- 端末間同期と履歴が公式にまとまっている。
- Git を使わない運用向け。料金と「Git で十分か」を比較して選べばよいです。

---

## 5. セキュリティで気をつけること

| 項目 | 内容 |
| --- | --- |
| リポジトリの公開設定 | 個人のノートは **Private** にする（GitHub 手順マニュアルでもプライベート作成を前提にしています）。 |
| API キー・トークン | `.env` や「パスワードメモ」を Vault に置く場合は、**誤って Git に載せない**よう `.gitignore` に追加するか、Vault 外で管理する。 |
| 07_Swipe | 他人の著作物の取り扱いに注意。バックアップ先へ送ること自体は問題になりにくいが、**公開リポジトリにしない**こと。 |

---

## 6. 復元のイメージ

1. **GitHub 上の履歴** … ファイル単位で過去版を確認・コピー（GitHub 手順マニュアルの「バックアップから復元する方法」参照）。
2. **Obsidian のファイルリカバリー** … ローカルのスナップショット（コアプラグイン）。Git と併用すると安心です。
3. **OS / 手動 ZIP** … 丸ごと巻き戻し・特定日時のフォルダ差し替え。

「どれを使うか」は失った原因（誤削除・ディスク故障・同期競合など）で変わります。**普段から GitHub に push しておく**と、多くの場面で一番取り戻しやすいです。

---

## 7. 困ったときの参照先

| 状況 | 見るもの |
| --- | --- |
| GitHub ＋ Obsidian Git の初回セットアップ | [データのバックアップ（GitHub設定）](マニュアル%20-%20データのバックアップ（GitHub設定）.md) |
| `git` / `gh` が見つからない | 上記マニュアルの「困ったときは」 |
| 同期でファイルが二重になった | クラウドの競合を疑い、**一度どちらが正か決めて**から片方を整理。以降は Git 主運用を検討 |

---

## 8. 更新履歴（メモ用）

| 日付 | 内容 |
| --- | --- |
| 2026-05-12 | 初版（総合方針＋既存 GitHub マニュアルへのリンク） |
| 2026-05-12 | GitHub 手順マニュアルの拡充に合わせ、上記リンクの説明文を更新 |
